language: cpp
compiler: gcc

os: linux
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-10.1

script:
  - export CC=gcc-10.1
  - export CXX=g++-10.1
  - cmake --version
  - cmake CMakeLists.txt
  - cmake  --build . --target neat
